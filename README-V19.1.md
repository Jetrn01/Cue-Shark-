# PottersMate V19.1 — Public Page CSS Fix

V19.1 fixes the public tournament/player pages appearing as unstyled HTML.
The page CSS was present but was not mounted into the React page; V19.1 renders the CSS with a style tag.

No database changes.
Existing organiser dashboard, QR scoring, TV display, draw/progression and branding are retained.
